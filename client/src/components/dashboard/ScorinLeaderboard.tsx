import React from 'react';

const ScoringLeaderboard = () => {
  return (
    <div style={{ border: '1px dashed #ccc', padding: '1rem' }}>
      <strong>ScoringLeaderboard</strong> (mocked content)
    </div>
  );
};

export default ScoringLeaderboard;
