import React from 'react';

const HotelCard = () => {
  return (
    <div style={{ border: '1px dashed #ccc', padding: '1rem' }}>
      <strong>HotelCard</strong> mock component
    </div>
  );
};

export default HotelCard;
